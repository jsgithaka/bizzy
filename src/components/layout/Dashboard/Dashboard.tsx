import { Aside, Header, Main } from '@structuralComponents';

export default function Dashboard() {
    return (
        <body>
            <Header />
            <Main />
            <Aside />
        </body>
    );
}
